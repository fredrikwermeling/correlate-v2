- the clsuter gene and cell lines should be on the same row. the drop down linked to the clustering is not wrorking as i want now it just seems to simplify the drawn lines, i wsa thinking that there probably exist a setting to change how hoigh resploutin is invovled in the vclustering?
- the heatmap function from netwoork doenst work on the cell phone and perhaps not the ipda
- in heatmap, the show median is deppending on selecting something to sort by, theis is not correct. the median is unrelated to the sortinhhg
- in heatmap (comming from network), it shouldn´t close the heatmap if i by accident press outside the heatmap popout
- it should be possible to go from the heatmap to the wiki double clicking (?) oir some other way to clidck a column in the heatmap. I also asked several time for the possiblity to click a gene in the yaxis to silience it. could it be possible to select groups of cells (actviating a function fro group or gate A, and B) to select cell s in a or b and to allwo for copying the cell lines from those or to open the cell line browser inspect function absed on group A and B.
- when I uploaded the export for AI file to chategpt, it asked what to do woth it. make sure any LLM understnad what to do. It seesm like chatGPT doesnt undertand to open the j.son.gz file
- bu deafult cluster also by celll ine. and also, for the number of treas/cluster to color, suggest what is best.




== PHONE, still open (2026-08-13, app at v.87.98) ==

A. "Rank by most genes / strongest r" in the network, reported as still visible
   in the gene-set-with-stats input. NOT DONE: could not find the control in
   index.html, searched rank / sort / "most genes" / "strongest r". Get the
   exact place it appears (a screenshot of that panel would settle it) before
   hiding anything.

B. Network nodes and edges are hard to hit with a finger. vis-network has no
   hit-tolerance option, so the fix is drawing nodes and edges bigger on a
   phone (netNodeSize / edge width defaults scaled when innerWidth <= 640),
   not a one-line setting. Started and backed out rather than ship a comment
   describing something the code did not do.

C. Everything else from the phone pass shipped, v.87.85 - v.87.98. NONE of it
   has been seen on a device: verified only as parsed, scoped to <=640px, and
   not affecting desktop. Ask for fresh screenshots before trusting any of it.

PHONE / CELL PHONE PASS  (open, 2026-08-13, app at v.87.91)

Screenshots for all of this are in ~/Downloads, IMG_3149-3157 (small phone).
Earlier set IMG_3138-3145. Read them with the Read tool, they are the only
real verification available, see the testing note at the bottom.

== PHONE PASS: all reported items now shipped (v.87.96) ==

Nothing from the screenshots is outstanding. Shipped v.87.85-v.87.96:
row labels, tap-to-identify, one scroller in the browser, bigger controls and
44px close buttons, oncoprint cells 15x30, nav row wraps, Options two-per-row,
solid popout backdrops, every export hidden, five thresholds hidden running
defaults, collapsible panels that hold their state, browser boxes sizing to
the row, cell-line detail above the list, Gene pairs / Clusters scrolling
sideways, and one height for the correlation popout's boxes.

NOT VERIFIED ON A DEVICE. Everything CSS-side is verified as parsed, correctly
scoped to =<640px, and not affecting desktop, but the window in this
environment will not drop below the breakpoint. Ask for a fresh set of phone
screenshots and re-check against them before calling any of it done.

Known not addressed, and believed not to be the app: the clock and battery
showing over the top of the page is Safari drawing a translucent status bar
over scrolled content. env(safe-area-inset-*) reads 0 without
viewport-fit=cover, and setting that pushes content further under the notch.
If it matters for a demo, give the top element a solid background.

== HOW THE PHONE BUILD WORKS (so this is not re-derived) ==

- One media query, index.html, "@media (max-width: 640px)" starting ~line 2081.
  Everything CSS-side lives there. `.mobile-hide { display:none }` is the
  general opt-out; ids are listed explicitly for the rest.
- Controls are enlarged there (>=40px, 15px text). Before v.87.86 they were
  being SHRUNK below desktop size.
- Collapsibles: app.js `_setupPhoneCollapsibles(root)` + `_armPhoneCollapsibles()`,
  armed once at startup and re-run by a MutationObserver, because these panels
  are rebuilt by several paths and a single per-render hook missed them.
  Hooks on `.panel-heading` (network) and `.inspect-controls .control-box-compact`
  (correlation popout).
- The five threshold controls are hidden and their DEFAULTS run: cutoff 0.50,
  slope 0.10, min cell lines 25, coverage 100, p < 0.001. The inputs stay in
  the DOM carrying those values, which is why the analyses still read them.

== TESTING NOTE, READ BEFORE STARTING ==

The browser in this environment will NOT resize below the breakpoint;
resize_window reports success and innerWidth stays 1420. So:
- To exercise the JS phone branch:
  Object.defineProperty(window,'innerWidth',{configurable:true,get:()=>390})
  before the app boots, or set it then re-arm. This does NOT activate the CSS
  media query, so CSS changes cannot be seen, only verified as parsed and
  correctly scoped.
- Real verification is the screenshots. Ask for a fresh set after shipping.
- Bump ?v= in index.html whenever app.js changes or the phone will run a
  cached script (this bit me once already this session).


TO DO  (pruned 2026-08-13, v.87.78)

Everything above the ARCHIVE line below was gone through item by item on
2026-08-13. Items confirmed working were removed and are listed at the bottom
under "verified done", so they are not re-reported. What is left is open.


== OPEN, small and concrete ==

1. Scatter legend for lineage / sublineage / disease is cropped on the left or
   right. Needs a real layout fix, not a nudge.

2. Correlate popout: a label added by clicking a cell line cannot be removed by
   hovering it, the way the gene effect popout allows. (Double-click removal
   exists; the hover affordance does not.)

3. Gene effect plot: clicked cell-line labels overlap when points are close and
   cannot be dragged apart. They can be dragged in the scatter.

4. Gene effect settings: the x and y axis titles cannot be edited, unlike the
   scatter's.

5. Network: text can be cropped on Run. Nodes, edges and text should always be
   fully visible after a run.

6. Network spread: with nodes locked and moved by hand, lowering the spread
   snaps them back to their pre-move positions.

7. Network spread should start at 100; the font / node / edge sliders are too
   small, roughly 30% smaller than the original look.


== OPEN, needs a decision before building ==

8. Drug-treated cell lines: describe them better on the card, and add a quick
   filter for drug-treated / naive PAIRS. Related to item 12 below.

9. Cell line wiki: add a "history" section, with links to central references
   for the line. Prompted by RVH-421 / RVH-421 Dab-R.

10. DepMap publishes a list of artefact genes. Worth surfacing as a warning?
    (The app already carries a CN-artefact list, which is a different thing.)

11. Genes measured in very few cell lines are over-represented in every
    ranking, and 26Q1 made it worse (visible in Find correlates). Options: a
    global "gene needs data in >= N cell lines" filter, or excluding the thin
    26Q1 additions. A per-view coverage floor already exists; the question is
    whether to raise it and apply it everywhere.

12. RVH-421 Dab-R: DepMap dropped a character, so the derivative reads
    RVH42DABR rather than RVH421DABR. This is the same underlying data problem
    as the one dangling entry in matchedPairs, where the derivative is not in
    the cohort it is paired into.

13. Cell line browser inspect: two more "compare with" modes, one selecting
    lineages from a tree, and one more that was cut off in the original note.
    (The tree picker exists as "Pick tissues / diseases"; check whether this is
    already satisfied before building.)

14. EBV and other infection status as a per-line annotation and a quick filter.
    Virus status IS in the data and IS a curated collection; what is being
    asked for may already exist. Confirm against the browser before building.

15. Clicking a gene in the cell line browser should open the gene effect view
    on the view that matches the inspect table, and group order should not
    change between runs (e.g. increased vs decreased response).

16. Should the data be updated to the latest DepMap release?


== VERIFIED DONE on 2026-08-13, removed from the list above ==

- Disease filter not cleared by the browser's Reset. Tested: Melanoma -> 55
  lines -> Reset -> cleared, 1208 back.
- "CI" in the scatter highlight matching 95 lines. Tested: CI now matches
  exactly 1 line; CI, MEC1, PGA1, WAOSEL matches exactly 4.
- Gene effect popout could not switch to expression when opened from a network
  node. Tested: switches and redraws.
- Mutation inspect jumping out of mutation mode when switching GE / expression.
  Tested: stays in mutation mode, title follows the measure.
- Cell line inspect claiming "48 vs 12" when it compared 27 vs 3. Coverage is
  now stated in the title and as a warning.
- Cell line inspect p-values too harsh on small groups. A Welch / rank-sum
  choice now sits in the sidebar.
- Per-cell-line CSV from the cell line inspect. Exists, and states its scope.
- Export for AI written for someone who has not read the file. Rewritten, then
  audited by assistants that had never seen the app.
- Custom export for AI in Options > Other, with a pasteable request block.
- Growth rate as a scatter axis. Present as an axis type.
- Lineage / subtype / disease filters in every filter panel. All five present.


---
⏺ Here's the full accounting, ordered by your to do file so you can walk it top
  to bottom. 35 versions today, v.84.62 → v.84.96.

  Items 1–34

  1. Copy-plot button — Copy beside Export image on network, scatter, tissue
  chart, mutation table. Plotly charts via Plotly's PNG writer, others via
  html2canvas. .85
- for network, also inlcude the legends in the copy. also, dont emphasis this button differntly than the ones next to it.

  2. Gene-name suggestions on typo — all 11 "not found" alerts replaced by a
  popout with closest matches, clickable to copy. IL4RA→IL4R,
  ABL→ABL1/ABL2/ABLIM1, Trp53→TP53. Also fixed the matcher: it scored a
  transposition as two edits and took the first three hits in list order, so
  CDNK2A buried CDKN2A. .84
- Good. if click the suggest match it should appear in the input box, not be copied. also, i dont undersrtadn what you mean with CDKN2A buried CDKN2a

  3. Alteration dropdowns, no WT/mut selector — picking a gene applies
  "altered"; WT / one copy / both copies moved onto the chip. .72
- ok

  4. Wiki fusion wording — rewritten. .76
- i dont understand 

  5. Fusion/amp-del grids — three faults: filters matched nothing (rows were
  looked up in the hotspot matrix, where a fusion name or "CDKN2B del" doesn't
  exist); labels overran (pane fixed at 72px for gene symbols); Apply now
  prominent. BCR-ABL1→12 lines, CDKN2B del→295. .73
- looks ok now

  6. Quick filters button + non-lineage cancer types — button was broken by my
  own CSS-class change .73. CLL genuinely unreachable: it's an Oncotree-only
  entity, filed under "Mature B-Cell Neoplasms". 48 entities are in that
  position, so there's now an exact-disease picker with all 165. .95
- ok

  7. PGA1 not showing KRAS — it does carry a KRAS hotspot, but the summary read
  only DepMap's inferred variant list, where PGA-1 has no hotspot entry. Now
  falls back to the matrix. .94
- we ahve worked further on this. but in general the summary should indicate the most important potential drivers of the cancer. if it is an atypical mutation it should still be stated but the atyupical nature indicated.

  8. Network export DPI — confirmed: the export copied the on-screen canvas and
  stretched it, so detail was identical at any DPI; the legend improved because
  it's drawn directly. Network now redrawn at export size, 9× the pixels at 3×.
  .94
- good. the copy button is also good but should contain the legend (below the network). also that button should not be differnt from the other export buttons (grayish), and it should be called copy network. 

  9. American English — 425 changes, verified no element reference broke. .75
- ok

  10. Saved figure not restoring colour-by — verified fixed by the restore
  allowlist and picker scoping; 5 picked tissues → 7 traces before and after.
  .82
- i dont understand

  11. Scatter CSV gene1/gene2 — columns named after the genes, plus a
  ShownOnPlot column since the file holds more rows than the plot. .82
- i dont understand

  12. Compare-by-hotspot on 2 vs 1+2 — checked, both scans already group 1+2. No
  change needed. .79
- ok


  13. Correlation filter layout — fusion filter now sits with the other three,
  Compare on its own row, hairlines between groups. .92
- ok

  14. Gates: Enrichr, AI export, PNG/SVG buttons — Enrichr A/B was dead because
  it asked for its cutoff via a blocking prompt(); now uses the panel's p-value
  box. Separate PNG/SVG replaced by the standard dialog. Gates gained Export for
  AI, pre-filled with the "what separates A from B" question. .80, .95
- i dont find this. in genral you need to specify better what you are commenting on. where in the app is this related to?

  15. Overlay counts not scaling to filters — they followed tissue/subtype but
  not mutation/fusion/CN, and ran before the plot updated. KRAS reads 174 of
  1,147 unfiltered, 100 of 574 with a TP53 filter. .78
- I lack context and dont know what this relate to.

  16. AI export defining its contents — each source now names what it
  contributes. .82
- ok

  17. Overlay heading cropped / not editable — line now wraps; title/subtitle
  split fixed so wrapped text is reachable. .90
- I lack a bit of context here, but if this relate to the correlation popout overlaying eg a hotspot mutation and viewing it as 3 panels, it looks better, but in the settings i can still not edit the text of the idividual plots only the proper title of the plot. 

  18. Correlation title naming GE/expr/CN — added, wraps when long. .76
- look at the screenshot from 06:58, the title doesnt look good in corrleation

  19. Gene hover text — leads with function. CDKN2A no longer opens with
  transcript-variant boilerplate. .84
- ok. 

  20. Expression correlates — retired, panel unreachable. Code left in place —
  91 references, some shared with mutation-analysis inspect. .96
- ok

  21. Shift-click inconsistency — both open the wiki now. .77
- good. In the correlation it still states that shift click will go to cell line browser, it should be to cell line wiki (which should also be the text elswhere this function exist).

  22. Gene-effect filter chips — clickable like the browser's. .89
- I lack som context here and dont follow the comment, what is filter chips?. But in the gene effect (and mutational analsis insepct), the filters should work as 

  23. Colour-by-GE not rescaling — node colours already followed visible genes;
  all four legends didn't. .77
- it seems to work, but when removeing or adding back genes using the remove mode, I need to reselect the scale to "network only" for it to change the scale of the coloring scale. this should be automaitic if the color scale lowest is -2 and i click to remove the .2 gene, the scale should autmaitically scale to the new reality without the removed gene. and if the gene is returned, the colors should directly scale accordingly.

  24. Network node spacing — Spread slider, 40–220%, independent of font/node
  size. .85
- we are working on this outsied of this todo list and you have been struggling. 

  25. Zoom/fit warning — notice with node count and a Fit button. .83
- this doesnt seem to work. when the network is zoomed so nodes or text is outside there is no warning and the network is even sometimes generated with nodes or text not fully visble (a netweork should never be generated like this when pressing run, it should be possible if the user zooms in, but not when user press run, then all should be visble. perhaps its the spread function that interferes with this, having the spread at the highest number makes some nodes/text outside the visible area).

  26. Network export whitespace — the filter banner's separator ran edge to edge
  and pinned the crop. 1200px → 530px. .83
- ok

  27. Lineage filter lingering — the banner read the live dropdowns, so changing
  a filter relabelled an unchanged network. Now captured at run time. .81
- i dont follow the context for this comment. 

  28. Reset all emphasis — solid green, dark border. .76
- ok. it looks good in general except for the reset all in network shuld have the same green as other things, not the dark green. its emphasised by being green and by having a sublte black border. no need to add another green nuance. 

  29. AI export naming a model — reworded. .76
- ok. i note in the export for ai in the gene effect that it suggest to ask what is the differnce in gata a and b. first there are no gates in gene effect (only in mutational analysis. and since gates are likelly not going to the version to be publishe, make a more genereic comment than top talka bout gates. 

  30. Mode switch not clearing results — network, tables and summary now
  cleared; gene list kept. .79
- it doesnt look to be implemented. if i first work with a network/gene list, and then go the the mutation analyis, the gene list is seen but grayed out. in general i´m thinking tha tthe mutational analysis could be partly changed to not keep too much of the network analysis parts visible. its a bit confusing, and could benefit from a more dedivcted look. perhaps the right side of the app could show the tabels setup that appear when pressing run in mutation analysis, but without any genes (as run has yet note beem pressed). also the 2. input gene set could be completelly hidden when selecting the mutation analysis. its confusing. 

  31. Mutational analysis over expression — Measure toggle. On BRAF: gene effect
  gives BRAF/SOX10/MAPK1/DUSP4, expression gives CD24/SPINT2/UCHL1. Top hit
  recomputed from raw matrix, exact match. .93
- this is not solved. if i do mutational analysis and switch to     .  also, if im in the mutation analysis inspect, and change eg the GE to expression, the poput swithches from the mutational analysis inspect tot the standard gene effect/by tissue. if im in the gene analysis inspect mode, it should stay there independent of any changes i make until i press the by tissue/hotspot. Also, if im in the gene analysis and have the GE setting, and press inspect for a gene, it goes to the inspect and in the upper left corner it says the gene and GE. if i then go back to the gene analysis, select mRNA and press inspect for a gene the upper right corener stats the gene name and GE (not epxression). the graph might ocrrectly show expression, but the seldctor says GE. this is an example of where settings are saved. in general settings should not be saved.

  32. Title not fully editable — panel split at the first <br>, but that's also
  how a title wraps. .88
- not clear where you are refering to here. but all text (headisngs subheading, x y axis etc= should be possible to edit in the settings.

  33. Long subtype names — the 99-character Soft Tissue subtype took the whole
  margin; labels cut to 34 with full name on hover. .88
- ok

  34. Subtype colouring + names in a box — labelled cell lines now list under
  the plot with Copy names. Subtype colouring not added — the view already
  groups by subtype and I wasn't sure what you wanted beyond that.
- ok

  The YRDC block
  
  I checked it against the data rather than trusting it. All factual claims
  reproduced. One correction: their permutation-FDR recommendation would have
  endorsed the hit (FDR 0.17) on the split the app showed — it only becomes
  informative after the no-call fix (1.31).

  - No-call vs wild-type .74 — 74 lines have no RNA-seq; in Ewing that's 10 of
  27, all landing in "wild-type". Group means: −0.61 called, −1.46 genuinely
  negative, −1.86 never sequenced.
  - Partner flatness .86 — YRDC moves 1.13, its 10 partners top out at 0.11.
  - Permutation check .87 — on demand, capped at ≤200 cell lines (44s on a full
  panel, and the question doesn't arise there).
- what is your suggestion to solve this? is it a problem? 

  Earlier in the session

  Wiki correctness (.62): 374 lines contradicted themselves on CDKN2A; BCR-ABL
  claimed for 29 lines when 12 are real; copy numbers assumed diploid; missing
  data reported as findings. Wiki scrolling (.64, .66) — rebuilt as one ordinary
  scrolling page. PDF page numbers and clean page breaks (.65) — 6 of 8 breaks
  used to cut through text. Drug section implying a cancer type (.67).

  Two caveats worth your attention: three bugs I fixed in .73 were regressions
  I'd introduced in .70–.72, live for about an hour. And #20's code is hidden,
  not deleted — say the word if you want it properly removed.

✻ Churned for 47s
  
❯ are there any calls that you need inout in related to the latest todo list    

⏺ Yes — six where your answer would change what I do, and a few you'd need to
  check by eye.

  Decisions that change my next move

  1. Expression correlates (#20) — hidden or deleted? Right now the panel is
  unreachable but the ~500 lines are still there, because 91 references cross
  into the mutation-analysis inspect. If you want it genuinely gone I'd untangle
  the shared helpers first. If "not visible" is enough, we're done.
-remove

  2. Subtype colouring (#34) — what did you mean? The gene-effect view already
  groups rows by subtype. I couldn't tell whether you wanted colour applied
  within an existing plot (like the scatter's colour-by), or something else, so
  I did the labelled-names half and left this.
- looks good, also add version for disese coloring.

  3. The pathway verdicts. You said keep the 25 if the analysis is correct, and
  I audited it. But you also said the app shouldn't over-interpret. "Driver
  activated" / "Brake lost" are interpretations, even framed as hypotheses. Keep
  as is, or reduce to naming which pathway carries which alteration and let the
  reader judge?
- keep

  4. Permutation check capped at 200 cell lines. My call, because it took 44s on
  a full-panel split and the question doesn't really arise there. If you'd
  rather have it available anyway, I'd make it explicit opt-in with a warning
  rather than hidden.
- is this related to the cell line inspet? 200 cap is good

  5. The exact-disease filter (#6). You asked for "filters for cancer types not
  defined by lineage and subtype, eg CLL" — I read that as a general gap and
  added a picker with all 165 Oncotree entities. If you meant a handful of
  curated quick filters instead, that's a different build.
- good

  6. The standalone Oncoprint button. I removed it in v.84.70 when each
  alteration filter got its own grid. That means there's no longer a single
  "oncoprint over the whole current cohort" button in the analysis tab. Tell me
  if you miss it.
- good

  Please check by eye — I couldn't verify these properly

  Copy to clipboard. Clipboard writes need a real user gesture, which automation
  can't reliably produce. The code path runs clean and fails gracefully, but I
  have not confirmed an image actually pastes into an email. Worth one test.
- ok

  The PDF. I verified page count, page numbers and that breaks land in
  whitespace, but I have no PDF renderer here — I've never seen the file. Same
  for the network export at 1200 dpi: I confirmed 9× the pixels, not that it
  looks right.
- looks good.

  Network export at high DPI on a large network. I capped the re-render at 4×,
  but a 200-node network at 1200 dpi will build a large canvas. If it's slow or
  fails on your machine, that cap needs lowering.
- ok

  Two things I'd raise unprompted

  "Oncoprint" naming. I made the plain description the title with (oncoprint) in
  grey after it. You asked whether the word makes sense — my answer was "right
  for someone who's met it, meaningless otherwise", but I never got your verdict
  on dropping the parenthetical.
- ok

  The three regressions in v.84.73 were mine, from v.84.70–72, and live for
  about an hour. All three were the same class of mistake: I changed how
  elements are shown or looked up, and didn't re-test the paths that depended on
  the old behaviour. If you hit anything odd in the browser toolbar, that's the
  first place I'd look.
- ok 





-------------------

could each plot also have a rapid copy graph button that can be used to copy a good version of the plot that could be pasted into eg an email directly, instead of exporting.
if adding a gene name in eg cell line browser, correlaion or gene effect, if the gene name is not correct, couold a popout with suggested names come up that are similar. i find my self eg writing il4ra, ewhen the real name is il4r.
the box for hotspot/fusion/amp/del in the cell line broweser should be a dropdown menu with all the mutations. but there shouldn´t be any selector for wt or mut. by defult, it should asumme its mutated/altered if selecting the gene.
in wiki "Gene fusions (also called translocations)" sounds strange. make sure that the language is clear and correct.
the fusision filter/oncoprint in cell line browser doestn work. The trext is overlapping with other parts of the oncoprint. Also, now filter is applied succesfully when i apply it (no cell lines selected). same is tru for amp/del. even if i select something that there should be cell ines for, no cell lines are shown.   Also, for the cell line browser oncoprints, make the apply button more pronounced so it's clear that this is the final thing to click.
the quick filter button doesnt work. Also could it conatin filters for cancer types not definedd by the kineage and subtype, eg CLL.
For PGA1 cell line in celll ine browser: "PGA1 is a Chronic Lymphocytic Leukemia/Small Lymphocytic Lymphoma cell line (Lymphoid) from a male, primary tumour. Notable: near-diploid." Why doesnt it highlight a KRAS hotspotmutation in the card ? this must be the most relevant finding to report?
when exporting the newtwork, it doesnt seems like the size and dpi of the export matters, the network looks similar in large 1200dpi as in medium 600dpi for the network exported as pptx. perhaps the legend is higher qulity but not the network.
use american english in the app, eg not colour but color. 
the json exportedd info related to images is good but not perfect, it doesnt perfectly recreat the iamge. it should constain all setting and foramting iamge for an exported image. for examople i wexporteda correlation with somne selected lianges, but the recreated(open json) file didn t have the correct selection of linages colored (it showed lal, when the exported image only had 5ish hifghlighted linage.
exported scater csv file only relates to gene1 and gene2, it needs to better describe what is gene 1 and gene 2. 
the compare hotspot in the correlation is a bit miss guided in that it compares based on 2 mutations, but if i click the mutation, the filter 1+2 mutation appears with another result (infered by the 1 mutation i guess), i guess it makes more sense the the list sorts byt 1+2 mutations instead of only 2. 
In correlation tab, the fusion filter should be moved up below the hotspot filter box, and right to the cn amp/del filter. the compare box could be made into a one row filter with everything on the same row (similar to gate. the differnt parts could have a subtle seprating line to make it more intotuive that differnt boxes are differnt. eg plot size and axis ranges are togeheter, tissue/hotspot/CN and fusion and comare are together etc. 
The gate function should have a dedicated export for AI, essentially asking the question what is explaining the differnce in the cells in the gates, not focusing on what is shown on the actuall plot, but rtying to use the other omics data to explain it. woring with the gates i see that there are png and svg export buttons. i thiought we agred on having a dedicated export button everywhere allowing the user to select how to export. the emnricher A and B for the gates (im in correlation tab, but same should be true everywhere there are gates) doesnt work. 
The correlation fusion and hotspot overlay needs to scale to the filters, ie only suggesting filters and # of cell lines absed on the filtered cells. in general all over the app it should scale number s to filters. 
When exporting for AI, the popout should define which data that will be expoerted. 
the fusion and hotspot overlay at color 0/1/2, the information in the hedeing related to the overlay, is too long and cropped. also, it si not pissble to edit the text related to  the overlay (fusion/hotspot, in both 0/1/2 and 3 panel) in the settings, it should be possible.
The correlation title is only mentioning the gene name but not if it is GE expression or CN. 
When hover over a gene the information that apears is not always intutive, eg for CDKN2A "This gene generates several transcript variants which differ in their first exons. At least three alternatively spliced variants encoding distinct proteins have been reported, two of which encode structurally related isoforms known to function as..." apears. the first text would better focus more directly on the function of the gene.
the expression correlates should perhaps be removed. such things could be explored with gates ot with export for AI.
in the gene effect tab, shift clickig a cell line results in goign to the wiki, while in the correlation tab it goes to the cell line browser. It hink i prefer the wiki. but again, functionallitites should be cosnsitent over the app. 
the function of the filters in the gene effect should be the same as in the cell line browser, including making it possivble to click the colorfull button that appear related to a filter allowing to click and change it eg from mutated to WT. 
In the network, color by GE should scale to the genes shown on the plot, so if i use the remove mode to remove a node, the scale/color for color by GE should adjust. 
could the network have a function that allwos for gradually increasing or deacreisng the diffeernece between th differnt nodes, i guess the settings ahave a distance somtime syou might want to make the figure more cndesed to take less space. 
for network, the app should warn if anything is zoomed so things are outside the visible window. by deafult zoom so everything fits into the n visible window, eg if i change the font size, maek sure everyhting fits into the poage. 
in the network, if i use a filter, the exported image has a lot of exptra white area on the sides, try to crop as much unnessecary white away (leavign a bit so it looks natureal).
in network, if aselect a linage filter, it seems to linger if i later select andother linage fitler, or at least a subtype was written as a filter that no longer was shown after applying another. 
Make the reset all button more emphaziesed green and witha  stronger black border. 
For the AI export, dont manetion which model to use. IT could state that the functionallity has been optimized for Claude, but should use for other.
when pressing mutational analysis, any networks or gene list prsent should be removed, the app should reset. 
Could the mutational analysis also be changed to a gene expression version, eg showing a table of genes that changes in expression based comparing BRAF wt vs BRAF mut? it could be a subtle button, with emphasis on GE.
in the gene effect when looking at the selected cells GE vs genetic alteration, the title is not full editable in the settings part, the editable text is only the firest rrow of the title, and in this case the title was several rows (also0, use the full width to not make several rows on the graph title/heading/subheading unless needed. 
in the gene effect, if a cancer type (subtype) has a very long name eg in soft tissues one subtype is super long name), the whole graph is not looking good, this needs to be solved in abetter way. 
could the gene effect tab also allwo for highlighting/coloring subtypes of cell lines. also could highlighted cell lines (highlighted by clicking the names apear in the "highlight cell line box). in this way i could copy the cell line naems in a good way. 


I used Correlate V2's gene-effect view to look at YRDC dependency across 25 Bone / Ewing sarcoma cell lines from DepMap 25Q3, stratified by EWSR1-FLI1 fusion count, then exported the JSON and handed it to an LLM for analysis. The export format worked well and the precomputed extras (tissue summary, mutation summary, variance warning) were used as intended. The problems that surfaced were in how the app frames results, not in the export schema.
The headline number looked strong: WT mean -1.74 (n=13) versus fusion-positive -0.61 (n=12), Welch p = 1.6e-5, and YRDC was the single most differential gene genome-wide. Two things undercut it on closer inspection.
First, the WT group was not a fusion-negative group. Eight of the 13 lines had no RNA-seq at all, so the clinical fusion caller could not emit a call and they defaulted to WT. One of them, TC32, is a textbook EWSR1-FLI1 type 1 line, and it sits at -1.61, which inflates the gap rather than diluting it. Restricting to lines from a single screen source and correcting the known misannotations shrank the comparison to 3 EWSR1-ERG lines versus 14 FLI1 lines.
Second, the genome-wide rank was uninformative at this sample size. Permuting group labels showed that a 5-versus-12 split yields on average 3.5 genes as extreme as YRDC by chance, against 3 observed. Empirical FDR around 1. The rank looked decisive and meant nothing.
The independent tell that this was screen noise: YRDC's only known function is making TC-AMP for the KEOPS complex, and every KEOPS subunit (OSGEP, TP53RK, TPRKB, LAGE3, GON7) was completely flat across the same split. That check took manual work to run and should be one glance in the app.
All three changes below aim at the same thing: the app currently makes noise look like signal in small, tissue-restricted cohorts, which is exactly the regime people use it in.
1. Separate "called negative" from "not callable".
Add a third tier: WT / mut / nocall, where nocall means the evidence the caller needs is missing rather than negative. Render it visually distinct in the plot, exclude it from default group statistics, and surface a banner when non-empty ("8 of 25 lines have no fusion call: no RNA-seq available"). Applies to any stratification depending on a data type a line may lack.
2. Add a permutation FDR to the genome-wide differential ranking.
On the gate-comparison and stratified gene-effect views, permute group labels 200 to 1000 times over the complete-case matrix, recompute Welch t genome-wide each time, and report expected-hits-at-observed-t alongside the rank. Also report the marginal per-gene permutation p for the focal gene, labelled clearly as valid only for a pre-specified gene, not one picked because it looked striking in the plot.
3. Add a partner co-membership sanity readout.
For a focal gene with known complex or pathway partners (the export already layers curated complexes, CORUM, and Reactome), show whether those partners move on the same split. A focal gene separating at p = 0.0002 while its entire complex is flat is the strongest available signal that the hit is noise, and right now the user has to compute that by hand.





older.

The app looks good on the functionality and UI. Here are a few things I found.

1.	In the Analysis mode (building a network), view controls settings are not getting incorporated properly
2.	In the Mutational analysis mode, once inside "inspect" changes in filters do not work properly/inconsistent
3.	In the export settings (everywhere), scaling of Title and Subtitle is not consistent. Add option to change text beyond the title and subtitle
4.	In screenshot option, the full content option captures only the .json file not the image. Add text saying a .json file will be downloaded alongside the image. 
5.	In the correlation mode, when using find correlate button add number of cell line along with the |r| value for each gene. 

Descriptive notes for the points above:
1.	Changes made in Filter banner are not visible real time but getting incorporated in exported images, changes in Legend are changing real time but not getting incorporated in exported images)
2.	Inspecting gene effects through the mutational analysis mode sometimes makes the app conserve filters applied during analysis. Resulting in inconsistent results when changing filters in the inspect mode specifically (Is not visible while using Gene effect option directly)
3.	App seems to be having problems with remembering what the Title and the Subtitle is, thus having problems with any change in text/font size. Not a big issue. No option to change text about the stats on number of cells analysed etc. 
4.	Visible area screenshot works fine. Full content is having issues
5.	The top hits almost always seem to be based on 1-3 cells line which makes them more or less irrelevant. Having a cell line number count will help identifying which ones are worth looking into.

In general, the new export functions (image, screenshot, etc) have bugs otherwise the app is working well. The phone app works fine but I see that it is having problem with the UI placement. 

- Look at the correlation_network-2.pdf in download folder. the edge thickness needs to scale to the zoom. tsc1-tsc2 should be around 0.9, but it looks much smaller in the exported (while the on screen image seems to scale correctly).
- I find HLA in several of the hotspot mutations filters. 




- Export buttons in the cell line browser need to be renamed. The function should be to have one that focuses on one (or a few) selected cell lines and shows GE and expression for all genes (currently called "GE matrix"). One export that focuses on one cell line and gives all information about all genes for that cell line (including damaging mutation hotspots, fusions, deep deletions, etc.). This is currently called "all matrices". and one button that focus on one or a few genes (and gives all info about those genes (GE, expression, deep deletions, hotspot etc) for all or a slection of cell lines (currently called "export genes"). the export that gives extensive information about all genes in one cell line, should also be possible to export from the wiki for the cel lline with a button next to the "download PDF" in the wiki, eg export gene info ?? .csv. 
- in the wiki, there shold be the possiblity to export drug responses as .csv so the user could make there own graph. same should be true for the breast cancer gene expression data.  also, try to make the histograms in the wiki look a bit better, they look pretty primitive. 
- add a screenshot button to all popouts, this is to facilitate making figures for the manuscript or information material. It should thus be a high-quality export, including both raster and vector-based, that could go into publication. it should also allow for showing the full screen, or only the part that is visible on the screen as the button is pressed 
- in the past we discussed the HLA "hotspot mutations". I guess they are not real hotspotmutations, but anotated due to the complexity of this loci? i think we should not hightlight these as hotspot mutaitons, do you agree?
- the sort by drug response seems to have a problem. it puts broadly toxic drugs in the end of the list. I think they should be on top of the list. It could be highlighted like it is now that they are braodly toxic. 
- the sorting should only include: name (defulat), GE, expression, and drug response. the pothers could be removed for now.
- anytime exporting a 
- the "open" button should only be on the first page.   also, anytime you exportan an image a meta data file should be exported that contains all info about the image (filters, settings changes etc). the meta data could then be opened with the open button to recreat the image in the app. previously the open was used to open image files but with limit success (not all filters etc were captured). keep the possilbity with the image like before, but lets focus on using the meta data file for this purpose instead as thje main tool. 
- would it be possible to be able to have a button when opening a popout, to click open as new tab. in this way the user could have opened eg the GE for TP53, and then in the the other tab go back to the network and do soemething else without haveing to close the TP53 GE by tissue. is there a clever way to solve this. A button in the popout is my intitial thought, but i guess any time a popout is opened it could be in a new tab?
- on a computer with a small screen, the last items on the quick filters popout is not visible. it should always be possible to move popouts by pressing and fdraging with the moues. also, make sure all data can be seen in the visual space. 
- I also want to test to rearrange the app. the reference data should be kept, but the cell line, gene effects etc should be moved above the results with a green banner called Options. the other boxes  1. 2. and 3. should be grayed out when opening the app, and only activated if the user clicks a button called Analysis that should be first on the list. After analysis, it should be gene effect, correlation, cell line browser, open, and last reset. hopefully this redesign will make the 1. 2. and 3, header/bamnner to be aligned. 


Plan for v1 (that will be pushed to corrleta.cmm.se by IT department, and which will be basis for manuscript):
- intially thought to only be based on GE to limit size, but we now want to expand to be rather similar to V2, but not including dimension reduction in cell line browser, and gates in eg the correlate popout.  v1 should also contain the .json export options in differnt plasces, but not expression correlates.  



older:
in v. 1 (but probably in other apps too): In mutational analysis, selected eg BRAF hotspot mutation. If you select only TP53 in the oncoprint, the oblique hotspot mutation also changes. These should be separated.
in v. 1 (but probably in other apps too): analyis of a gene set and applied TP53 mut and BRAF mut in oncoprint. in the network, the legend in the header say TP53 level all, it should say WT or mut. 
in v.1 (but probably in other apps too): ehen using design mode, searched genes are darker green and collerated geesn are lighter green. thuis is good. but when change the size in the "Aa" settings, the öighter gereen color diseapear. 

Better long-term fix in your correlate app: Another item for Claude Code. The SVG lines probably use absolute stroke-width values. Add this to your prompt:
When generating SVGs for export, add vector-effect="non-scaling-stroke" to line/path elements if you want strokes to stay consistent, OR (preferred for Inkscape compatibility) make sure all stroke-widths are set as proportional values and include the attribute style="vector-effect: none" on the root SVG so that Inkscape scales strokes with transforms. Also, avoid using SVG transform="scale()" on groups. Instead, set actual coordinate values so that when the user scales in Inkscape, everything behaves predictably.
The encoding issue you're seeing (the garbled text like "ÝÜOOîß" instead of gene names) is a separate problem. That's a character encoding mismatch in the SVG, likely UTF-8 vs Latin-1. Add this for Claude Code too:
Make sure the downloaded SVG file includes <?xml version="1.0" encoding="UTF-8"?> at the top, and that the text content is properly encoded as UTF-8 when generating the SVG blob for download.
SVG export improvements for correlate.cmm.se
Fix these issues with the SVG download/export so that the files work properly in both Adobe Illustrator and Inkscape:

Encoding: Add <?xml version="1.0" encoding="UTF-8"?> as the first line of the exported SVG. Make sure all text content is properly UTF-8 encoded in the SVG blob. Currently gene names display as garbled characters (mojibake) when opened in Illustrator.
SVG dimensions: Set explicit width and height attributes on the root <svg> element that match the viewBox (e.g., if viewBox is "0 0 800 600", set width="800" height="600"). Currently files open tiny in Inkscape.
Stroke scaling: Make sure stroke-widths on lines and edges use values that scale proportionally when the user resizes in Illustrator/Inkscape. Avoid using transform="scale()" on groups. Instead, compute actual coordinate values so scaling behaves predictably.
Optional: Add an "Export SVG (outlined text)" button that creates a second download option where all <text> elements are converted to <path> elements using opentype.js (CDN: https://cdn.jsdelivr.net/npm/opentype.js). This ensures fonts render correctly regardless of what's installed. Keep the normal SVG download with editable text as the default.

Apply these fixes to all SVG exports in the app (scatter plots, network graphs, box plots, etc.)


Fixes 1-3 are quick and will immediately make your life easier in both Illustrator and Inkscape. Fix 4 is the nice-to-have that solves the font reflow problem we hit earlier. You could do 1-3 first and add 4 later if needed.








- would it be possible to export images svg png so they coudl contain some meta data, and allow for being opened again in the app? this would facilitate making small cahnges to an image, not having to remake them.
- In the network, when I cahnge the font size, it seems to scale based on how many many nodes there are in the network. I want font size to be the same everywhere in app, so eg 15 = 15. 
- when I change the text in eg subtitles in the text settings the row division is abolished. eg if the subtitles are in 3 rows and I remove something, they beocme 1 row.
- allow in cell browser to export GE and expression data for selected cell lines. Now there is such feature for GE and allwoin g to compare several cell lines. when downloading data directly related to the depmap data files like this, add in the downloaded file, which version of depmap the data comes from and how t oc cite the use. this is a differnt .csv export. also make tthe names of the exports more self explanatory. also include columns for damaging, hotspot translaocations. ie rows has genes, at least 5 columns with expression, GE, hotspo, damaging ´, translocations. 
- make correlation plot of expression vs GE effect for a selected cell line. make a table with filters (tissue, sub tissue, hotspot mutations , translocationm) that identifies genes with the delta of expression and GE, the idea being to find how gene expression and correspnding efect of KO is. Clicking a gene in such a table could highlight it in the correlation plot.
- when making any changes to a graph, eg shich gene is shown on x or y axis, make sure that the app doesnt change the settigns for the text eg font size etc. now it jumps to a deafult setting.
- in correlate_scatter, the box in the upper left corner for cahnging gene names (GE or expression) is too small, i cant see what i write. It needs to be bigger.
- in teh network, allow for Aa settings. also allow for toggling on/off a thin line around the differnt nodes.
-  The Correlate app exports SVGs (both network graphs and Plotly-based scatter plots) that render correctly in Chrome but have problems when opened in Adobe Illustrator: garbled text, incorrect stroke widths, and wrong colors. I need the SVG exports to be Illustrator-compatible. Please update the SVG export functions with these changes:
For the network graph SVG export:

Use explicit inline style attributes on every element, no CSS classes or <style> blocks
Set all font properties directly on each <text> element: font-family="Arial, Helvetica, sans-serif", font-size="14" (as a number, not "14px")
Use absolute stroke widths as numbers, not px units (e.g. stroke-width="2" not stroke-width="2px")
Use hex colors (#333333) instead of named colors or rgb() notation
Remove any CSS <style> blocks from the SVG, move everything to inline attributes
Make sure the <svg> root has xmlns="http://www.w3.org/2000/svg" and the XML declaration <?xml version="1.0" encoding="UTF-8"?>

For the Plotly-based scatter plot SVG export:

After Plotly generates the SVG, post-process it to: convert all rgb(r, g, b) color values to hex, convert all CSS classes to inline styles, remove <defs> clip paths that Illustrator handles poorly, and simplify the nested group structure where possible
Or alternatively, bypass Plotly's SVG export and generate a clean SVG manually from the scatter plot data using simple <circle>, <line>, and <text> elements with explicit inline attributes

General rules for Illustrator-compatible SVGs:

No CSS classes, no <style> blocks
All styling as inline attributes
Font sizes as plain numbers (no "px" suffix)
Colors as hex values
Stroke widths as plain numbers
Keep the SVG structure as flat as possible (minimize nested <g> groups)
Include <?xml version="1.0" encoding="UTF-8"?> declaration




- allow for exporting data for umap to visualize format? Probably too complicated.


- in umap, # of mutations in cells line. also ,not only square gates, also indiacte cells in gate with color and with letter A or B like in the scatter correlate version.
- in umap cancer cells gate analysis also allow for enricher.
- in correlate scatter plot, the box for gene name in x and y axis (with possibility to change the GE or expression)
- output the first page as svg as a button.
- make it possible to change the size of legend font, text font size, etc., for correlation scatter plot, for gene effect plots, and for mutational analysis inspection, and for network and the linked legends. lern from visualize https://github.com/fredrikwermeling/Visualize with the Aa and wheel settings.   
- The size of edge thicknesses in the network does not correlate to the size of the legend. 

- allow for exporting data for umap to visualize format?


- in cell line browser, allow for making umaps of cell lines based on GE and/or expression, include filters for tissue/subtissue, hotspot, fusion. allow for separat graphs separating by a hotspotmutation, fusion or damaging mutation.  the  cellines should be possible to select based on where in a cluster they are (either by seecting cluster1,2 etc or by using a gate. the selected cel llines should be possible to copy.
- a custom cell line fitler should be added to every place where filters are used, where celline sto include could be paseted ( which match with the output, bit .csv or copy to clipboard from the umap above).
- i want some whay to test gene expressions GE. could this be incldued in the correlate_scatter. ie allowing for looking at both GE and expression, also some sort of ranking feature showing if GE and expression correlate. Nt sure how to implement this 


- in the double plot with gates and differences in the eg GE of a gene in the differnt gated groups, before the export png/svg, could an inspect windwo open where the graphs can be changed, eg size etc, so the user can mmek them uiniform before exporting the image? In general i want more control over the graphs, could more graph settings be added (and the changes should involve the exported .svg. png). look at how https://github.com/fredrikwermeling/Visualize solves some of these thigns.
- the color by tissue legend that appear in correlation scatter, doesnt fit within the screen and becomes cropped. The screen should allow for every element to be shown properly.


- in a correlation plot, could two gates be setup to select all cells within those gatges and then comaparing them to each other, based on differences in GE, tisue type, subtype or hotspot mutations?
- could correlate v2 also include other muations from depmap in the mutational nalsysis inspect. trying to identify what is differnt between eg TP53 wt lung cancerr cells that are happy with KO of TP53 and those that doesnt care




- analyse mode, correlations tab. if press by tissue, it doesnt allow to close and open by hotspot, instead it opens by tissue after a while.
- in analysis mode. if press correlations/clusters/summary tab, before pressing run, and then add a library and press run (while still in correlations/clusters/summary tab), the generated network looks strange. 
- are there repositories of mouse cell lines that could be included?
- press graphs in eg Gene effect by tissue data set, allow to press the cell and have the name labeled (and it apears in a list above the graph with a x that could be pressed to remove the name from the graph. this would also integrate with the serch name function. also, if comming from ecll line clicking of a cell line, the name should be integrated in the same manner
- cell line browser, also include info about subtype in the list deiplayed on the screen. Also allow for sorting the searched gene top-bottom, bottom-top in cell line browser. 
- export of .png/svg from mutational analysis:_inspect. crops output. Also perhaps not full on screen. 




- same genes in test data sets for input genes +/- stats.
- mutation analysis inspect make possible to change size of y axis.
- filtering should allow for - values

the size setting sfor the 3 plot vresion in correlate is off. 
the network moves around if selecting other tabs and then go back to network.
correlation table "by hotspot" doesnt work properly. also, both corelation and clusters tab should under analysis have a "by fusion" button


Divide this task into parts. make sure that you undertand what to do first.
- make a reset button that does the same as refreshing the page. perhaos in the reference data box 
- summary tab should include data on filters eg for hotspot mutaitons and for excluded cell lines.
- in anslysis/design mode, also inlcude a "tissues" button in set parameterrs like in mutation analysis.
- related to esclude tissues, also include a reset button
- in muational analysis table, by default sort by delta GE from low to high for (1+2 mutation).
- in mutational analysis inspect, the delta hotspot function, add warning about selecting for mutations and that it might introduce bias. have this infomation everywhere there is a option to select hotspot mutations
- in mutational analysis inspect, the delta hotspot/tissue function: also show the average GE in wt and in mut in the table
- correlate tab, also add by hotspot
- in clusters tab, also add button to add GE effect. I want the corelationa nd clusters tabs to hormonize which buttons they have and how they look.
- the circle for selecting design is larger than other modes on some computers (?). perhaps its because the design has less text and the selection circle adapts?
- would it be possible to do eg a go term analyis of the genes in any table, eg a go, or KEGG and reactome button to analysse any pattern. This could be a very usefull feature. 
- suggest synonyms is good. but could you also suggest genes, if the user miss spelled them. eg if write ABL, ask, sis you mean ABL1
Regarding the analysis mode, network tab:
- SVG export of network does not look like on the screen, the changes to node size, edge width, etc., are not followed.
- the color by GE should have the possibility (and by default) show the color intensity based on the genes shown on the screen(network) 
- move the show all input genes in the network above the show gene effect in the label.
- the app says CRISPR effect in some places (eg correlations) and gene effect in others. use gene effect everywhere.
- when going to summary/correlation/clusters tab and then back to the network, the network jumps out from the view of the screen, and one has to press fit-
- the layout button doesnt work well. the changes on the screen are not always usefull, and its difficutl to go back to the deafult setting.  
- if press color by GE and then "show all input genes in network". the color by GE is still clicked, but not in play. I want it to strill be activ and show GE also for the genes that are not in corerlations directly. (now this can be achived by clicking off and then on the "color by GE")


- for paper, the network color for GE or looking in the clusters tab could make a rapid way of understanding if some of the genes in your scren are not good drug targets if very strong GE.
